[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "anime-preference-sft"
version = "0.1.0"
description = "Anime Preference Understanding & Structured Recommendation Tool Calling"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
