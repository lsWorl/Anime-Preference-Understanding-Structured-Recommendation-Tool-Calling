# Anime Preference 项目实现学习手册

> 根据 2026-09-06 的本地代码编写。面向第一次接触该项目、具备基本 Python 语法知识的读者。文档解释当前实现，不代表理论审查已批准所有设计。源代码根目录：`D:\Study\anime-preference-sft`。

## 目录

1. [项目解决什么问题](#1-项目解决什么问题)
2. [目录、配置和阅读路线](#2-目录配置和阅读路线)
3. [理解代码需要的 Python 概念](#3-理解代码需要的-python-概念)
4. [单页 AniList 请求](#4-单页-anilist-请求)
5. [作品元数据校验](#5-作品元数据校验)
6. [JSONL 与 manifest 写入](#6-jsonl-与-manifest-写入)
7. [采集脚本与命令行](#7-采集脚本与命令行)
8. [Semantic specification 数据模型](#8-semantic-specification-数据模型)
9. [领域规则加载](#9-领域规则加载)
10. [确定性 Gold JSON 构建](#10-确定性-gold-json-构建)
11. [Canonical JSON 序列化](#11-canonical-json-序列化)
12. [约束结构签名](#12-约束结构签名)
13. [贯穿示例：从规格到输出](#13-贯穿示例从规格到输出)
14. [逐项阅读测试](#14-逐项阅读测试)
15. [运行与排查](#15-运行与排查)
16. [实现限制与理论审查问题](#16-实现限制与理论审查问题)
17. [函数索引与复盘练习](#17-函数索引与复盘练习)

## 1. 项目解决什么问题

项目名称是 **Anime Preference Understanding & Structured Recommendation Tool Calling**：理解用户的动漫偏好，将其表达为结构化查询，供后续推荐工具使用。

当前已经实现的是基础数据和结构化目标的构建。仓库还没有一个能接收自然语言、调用训练好的模型并推荐作品的完整应用。理解这一点，可以避免看到 `training`、`inference` 等目录后，以为这些功能已经存在。

当前有两条流程：

```text
流程 A：作品数据
AniList API
  → fetch_anime_page()：获得一页原始 Media
  → AnimeMetadata.from_api()：逐条检查字段
  → write_jsonl()：保存原始 Media
  → write_manifest()：保存本次运行摘要

流程 B：数据集目标
人工明确指定的 SemanticSpec + 已加载的 DomainRules
  → build_query()：校验、展开规则、构建 Gold 字典
  → dumps_query()：检查完整结构并生成固定格式 JSON 文本
  → build_constraint_signature()：提取硬约束结构标签
```

流程 B 并不直接调用流程 A。当前的规则文件是单独维护的配置，builder 也不读取采集的作品文件。

### 1.1 三个容易混淆的对象

| 对象 | 表达的事实 | 例子 |
|---|---|---|
| `AnimeMetadata` | 一部作品实际具有的属性 | 年份为 1998、集数为 26 |
| `SemanticSpec` | 要构造的数据样本明确要求哪些偏好 | 至少 2010 年、至多 24 集 |
| Gold Query | 模型未来应输出的结构化答案 | `year.min=2010`、`episodes.max=24` |

不能因为用户提到了某作品，就把该作品的所有 genre、tag 自动写进偏好。当前 builder 只处理已输入的 spec，没有这类推断。

### 1.2 当前没有实现的环节

没有随机语义采样器、自然语言模板生成器、LLM 改写、数据集切分、训练、推荐执行器或完整的 tool-call 封装。当前 Gold 是查询参数对象，还不是带工具名和工具调用 ID 的完整 API 消息。

## 2. 目录、配置和阅读路线

```text
anime-preference-sft/
├── pyproject.toml
├── README.md
├── configs/
│   ├── data.json
│   ├── data.yaml                  # 原有空文件；采集脚本不读取它
│   └── domain_rules.v0.1.json
├── scripts/
│   └── fetch_anilist.py
├── src/anime_pref/
│   ├── data/
│   │   ├── anilist_client.py
│   │   ├── io.py
│   │   ├── query_builder.py
│   │   └── constraint_signature.py
│   ├── schemas/
│   │   ├── anime_metadata.py
│   │   └── preference_query.py
│   ├── training/__init__.py       # 占位
│   ├── evaluation/__init__.py     # 占位
│   └── inference/__init__.py      # 占位
├── tests/
│   ├── test_scaffold.py
│   └── test_query_builder.py
├── data/{raw,interim,processed}/
└── outputs/
```

各包还包含 `__init__.py`，它们主要使目录成为可导入的 Python 包，没有额外业务算法。

推荐先读数据模型，再读消费模型的函数：`anime_metadata.py → anilist_client.py → io.py → fetch_anilist.py`；随后读 `preference_query.py → domain_rules.v0.1.json → query_builder.py → constraint_signature.py`。每读完一个模块，就看对应测试如何调用它。

`pyproject.toml` 声明 Python 最低版本为 3.10，业务运行依赖列表为空；当前使用 Python 标准库。构建后端是 setuptools，`src` 布局通过包发现配置识别。若执行 editable 安装，仍可能需要准备 setuptools 等构建依赖。

本手册中的命令统一从项目根目录运行。正文链接使用完整本地路径；源码更新后，函数名比历史行号更可靠。

## 3. 理解代码需要的 Python 概念

### 3.1 类型标注不等于运行时校验

`episodes: int | None` 表达设计意图，但调用者仍能在运行时传入字符串。`dataclass` 也不会自动阻止这种赋值。因此 `from_api()`、`build_query()` 需要显式检查。

`Any` 表示静态类型检查暂时不限制值，不代表该值已经安全。`Mapping[str, Any]` 用于标注；运行时应写 `isinstance(value, Mapping)`，不能写 `isinstance(value, Mapping[str, Any])`。

### 3.2 为什么单独排除 bool

Python 中 `bool` 是 `int` 的子类：`isinstance(True, int)` 为真。但“最多 True 集”没有本项目需要的数值语义。因此代码采用：

```python
if not isinstance(value, int) or isinstance(value, bool):
    raise ValueError(...)
```

### 3.3 None、空集合和零不同

`None` 在范围规格中表示“未表达该边界”；`0` 是实际数值。空列表表示对应 operator 没有值。序列化时 Python `None` 变成 JSON `null`。

### 3.4 tuple、list、frozenset 与 MappingProxyType

输入规格使用 tuple，例如 `("Mystery",)`；最后的逗号使它成为单元素 tuple。Gold 使用 list，因为 JSON 中相应结构是数组。规则 allowlist 使用 frozenset，便于检查成员且不能增删。`MappingProxyType` 提供字典的只读视图。

`frozen=True` 只阻止 dataclass 字段重新赋值，不会递归冻结其中的 list。`AnimeMetadata.genres` 和 `.tags` 仍是 list；`DomainRules.tag_groups` 则由加载器封装为只读映射。不要把所有 frozen dataclass 都称为“深度不可变”。

### 3.5 闭包与内部辅助函数

`build_query()` 内部定义的函数仅供这次构建使用。例如 `validate_set_constraint()` 可以访问外层 `operators`。这减少参数传递，但阅读时要同时看外层变量。辅助函数并不是另一个对外 API。

### 3.6 异常链和资源释放

`raise RuntimeError(...) from exc` 增加业务上下文并保留原因。`with path.open(...)` 或 `with urlopen(...)` 保证正常结束和异常退出时关闭资源；它不保证整个操作具有事务性，也不会自动删除已写入的半成品。

## 4. 单页 AniList 请求

源码：[anilist_client.py](D:/Study/anime-preference-sft/src/anime_pref/data/anilist_client.py)

### 4.1 ANIME_PAGE_QUERY 常量

这是 GraphQL 查询文本，不是一个函数。它声明 `$page` 和 `$perPage` 两个变量，请求 `Page.pageInfo.currentPage/hasNextPage`，以及 `media(type: ANIME, sort: ID)`。

作品字段包括 `id`、三种标题、`format`、`status`、`episodes`、`seasonYear`、`genres`、`tags{name rank}`。`sort: ID` 明确了排序，但并不等于远端数据库在多次请求之间被冻结。

GraphQL 与 SQL 不同，这段代码发送的是服务器支持的字段选择。当前客户端只接收结果，没有在本地执行查询语言。

### 4.2 fetch_anime_page()

```python
fetch_anime_page(
    page: int,
    per_page: int,
    *,
    endpoint: str = "https://graphql.anilist.co",
    timeout_seconds: float = 30,
) -> dict[str, Any]
```

`*` 后参数必须使用名称传入。函数返回 `response["data"]["Page"]`，不是完整 HTTP 响应，也不是只返回作品列表。

| 阶段 | 当前操作 | 为什么需要 |
|---|---|---|
| 参数检查 | page 是正整数；per_page 是 1–50 的整数；排除 bool | 提前拒绝明显无效请求 |
| 超时检查 | `timeout_seconds <= 0` 时拒绝 | 防止非正超时 |
| 构建 payload | query 文本 + variables 字典 | 让分页值与查询正文分开 |
| 编码 | `json.dumps(payload).encode("utf-8")` | urllib 请求正文需要字节 |
| 请求 | JSON Content-Type、POST、urlopen timeout | 发送至指定 endpoint |
| 解析 | 读取 bytes，UTF-8 解码，再 json.loads | 得到 Python 对象 |
| GraphQL 检查 | 顶层存在非空 errors 则失败 | HTTP 200 不代表 GraphQL 执行成功 |
| 结构检查 | data/Page 是 dict；media 是 list；hasNextPage 是 bool | 为分页调用方提供基本结构保证 |
| 返回 | page_data | 单条 Media 内容留给下一层检查 |

输入 `fetch_anime_page(1, 2)` 成功时，返回形状如下；这里是示意结构，不是本次实时 API 输出：

```python
{
    "pageInfo": {"currentPage": 1, "hasNextPage": True},
    "media": [{"id": 1, ...}, {"id": 5, ...}],
}
```

异常处理按顺序捕获 `HTTPError`、`URLError`，转换成 `RuntimeError`；已构造的 RuntimeError 直接重新抛出；其他请求阶段异常进入最后的宽泛捕获。

边界：客户端尚未逐项验证 Media，也未验证 currentPage 的类型与请求页一致。JSON 顶层未先检查 dict，非预期顶层值可能进入宽泛异常包装。直接调用客户端时，超时的类型/有限性检查不如 CLI 严格。Request 对象在 try 之前构建，因此不能把所有 endpoint 参数错误都概括成已包装的网络错误。当前没有重试、429 退避或断点续采。

## 5. 作品元数据校验

源码：[anime_metadata.py](D:/Study/anime-preference-sft/src/anime_pref/schemas/anime_metadata.py)

### 5.1 AnimeTag 与 AnimeMetadata

`AnimeTag` 保存名称和 rank。`AnimeMetadata` 保存一条作品的标准化视图。

| 原始 API 字段 | dataclass 字段 | 当前接受的值 |
|---|---|---|
| id | id | int，排除 bool；必需 |
| title.romaji | title_romaji | str 或 None |
| title.english | title_english | str 或 None |
| title.native | title_native | str 或 None |
| format | format | str 或 None |
| status | status | str 或 None |
| episodes | episodes | int 或 None，排除 bool |
| seasonYear | season_year | int 或 None，排除 bool |
| genres | genres | 必需的字符串 list，允许空 list |
| tags | tags | 必需的 list，元素转换成 AnimeTag |

### 5.2 AnimeMetadata.from_api()

这是 `@classmethod`，第一个隐式参数是类 `cls`。最后用 `cls(...)` 构造实例，调用方式是 `AnimeMetadata.from_api(media)`。

执行过程：

1. 检查 media 为 dict，避免对 list 调用 `.get()` 泄漏 AttributeError。
2. 取 id；缺失/None 或非整数时报 ValueError。
3. 取 title；允许缺失或 None；若是 dict，逐项检查三种名称。
4. 检查 format/status 的字符串类型，episodes/seasonYear 的数值类型。
5. 检查 genres 容器及每个元素。
6. 遍历 tags，使用 enumerate 得到索引，用于错误消息定位。
7. 检查每个 tag 是 dict，name/rank 必需，rank 为 0–100 整数且不是 bool。
8. 构造 AnimeTag 列表，最终返回 AnimeMetadata。

示例：

```python
media = {
    "id": 100,
    "title": {"romaji": "Example", "english": None, "native": "示例"},
    "format": "TV", "status": "FINISHED",
    "episodes": None, "seasonYear": 2020,
    "genres": ["Drama"],
    "tags": [{"name": "Time Loop", "rank": 80}],
}
metadata = AnimeMetadata.from_api(media)
# metadata.season_year == 2020
# metadata.episodes is None
# metadata.tags[0].name == "Time Loop"
```

读取缺失可空字段时 `.get()` 返回 None，因此“字段不存在”和“明确 null”在当前模型中被合并。函数不检查真实 taxonomy，不拒绝未知 format 字符串，也不限制 episodes/year 的业务范围。它不丢弃无效记录，而是整条失败。

当前标题赋值放在验证循环内部，会重复三次赋值，但不改变最终结果。genres 直接复用了输入 list；它不是输入数据的完整深拷贝。理解这些细节比把 dataclass 误认为自动清洗器更重要。

## 6. JSONL 与 manifest 写入

源码：[io.py](D:/Study/anime-preference-sft/src/anime_pref/data/io.py)

### 6.1 write_jsonl(records, path) → int

`records` 标注为 Iterable，可以是 list，也可以是生成器/迭代器。函数不使用 `len(records)`，而是在每条写入后递增 count，因此支持逐项消费输入。

核心逻辑：

```python
path.parent.mkdir(parents=True, exist_ok=True)
count = 0
with path.open("x", encoding="utf-8") as f:
    for record in records:
        line = json.dumps(record, ensure_ascii=False) + "\n"
        f.write(line)
        count += 1
return count
```

`parents=True` 创建中间目录；`exist_ok=True` 允许目录已存在。`x` 是独占创建模式，文件存在时抛 FileExistsError，避免覆盖原始缓存。

JSONL 是每行一个独立 JSON 对象，不是外层包一个数组。若字符串内部含换行，JSON 编码会转义该换行，因此仍可按物理行读取记录。空输入生成零字节文件并返回 0。

中文通过 `ensure_ascii=False` 保留为 Unicode 字符，再由文件编码写成 UTF-8。Windows 文本模式可能将行结束写成 CRLF；`splitlines()` 能正常读取，不能把 `"\n"` 拼接误认为严格保证 Unix LF 字节。

函数不执行 schema validation，也没有 `allow_nan=False`；它假设上游提供合适的数据。生成器异常、编码失败、磁盘错误都会向上传播。如果第二条失败，第一条和已有文件可能保留；没有返回 count 并不意味着没有产生文件。

### 6.2 write_manifest(manifest, path) → None

同样创建父目录和使用 `x` 模式。区别是写一个 JSON 对象，使用 `json.dump(..., indent=2)` 直接写到文件，并追加换行。

manifest 是运行摘要。该函数不检查摘要的字段或统计是否正确：统计由 main 负责，I/O 层只保存。已有文件拒绝覆盖；序列化或写入中途出错仍可能留下不完整 manifest。当前实现不是“临时文件写完再原子重命名”。

## 7. 采集脚本与命令行

源码：[fetch_anilist.py](D:/Study/anime-preference-sft/scripts/fetch_anilist.py)

### 7.1 模块初始化和 positive_int()

`Path(__file__).resolve().parents[1]` 从脚本所在位置定位项目根目录。把根目录下的 src 插入 `sys.path` 后，即使未安装包，也可直接执行脚本并导入 anime_pref。

`positive_int(value: str) -> int` 是 argparse 的转换函数。先 `int(value)`，再检查至少为 1。输入 `"2"` 返回 2；`"0"` 抛 ArgumentTypeError；`"abc"` 在 int 转换时抛 ValueError，argparse 会把这种转换失败作为命令行参数错误报告。

### 7.2 main() → int：配置阶段

| 参数 | 作用 | 默认或约束 |
|---|---|---|
| --config | JSON 配置路径 | 项目根目录/configs/data.json |
| --pages | 最多获取页数 | 覆盖配置 pages |
| --per-page | 请求每页条数 | 覆盖配置；1–50 |
| --output | 缓存目录 | 覆盖配置 output |
| --dry-run | 输出解析后的配置 | 不进入采集循环 |

解析顺序是“读取配置 → 非 None 的命令行值覆盖 → 验证 → 解析输出路径”。默认配置为 pages=1、per_page=10、timeout_seconds=30、output=`data/raw/anilist`，endpoint 为 AniList GraphQL 地址。

相对 output 基于项目根目录；相对自定义 config 基于进程当前工作目录。二者不是同一条路径规则。

配置验证检查 pages/per_page 的精确 int 类型、timeout 为正的有限 int/float、endpoint 为以 `https://` 开头的字符串。不会严格拒绝配置中的额外键，也不把 HTTPS 前缀检查当成完整 URL 验证。

### 7.3 main()：采集循环

初始 `page=1`，`total_pages_fetched=0`，`total_records=0`，`file_list=[]`。

每轮先检查 page 是否超过 max_pages，再请求当前页。所有 Media 通过 from_api 后，才把这一页的原始 media_list 写入文件。注意：校验生成的 dataclass 没有保存下来，JSONL 保留 API 字段 `seasonYear`，不会变成 `season_year`。

`filename = f"page_{page:04d}.jsonl"` 中 `04d` 表示十进制数至少四位、不足补零。第一页是 page_0001.jsonl。

写入成功后，使用 write_jsonl 的返回值累计实际记录数，实际页数加一，文件路径加入列表。如果 hasNextPage 为 False，立即退出；否则 sleep(1)，page 加一进入下一轮。即使已写完配置要求的最后一页，如果 hasNextPage 为 True，也会多等待一次才在循环顶部退出。

main 使用 `.get('media', [])` 等默认值；依赖客户端此前已检查对应字段。默认值不是独立验证，若未来换了未验证的客户端，这里可能把缺失数据误当成空页。

### 7.4 main()：manifest 与退出

| 字段 | 当前含义 |
|---|---|
| fetch_time | 写 manifest 前的 UTC 时间，ISO 8601 含 +00:00 |
| endpoint | 实际使用的 endpoint |
| requested_pages | 本次配置的页数上限，不是实际请求次数 |
| actual_pages | 已成功保存的页数 |
| per_page | 请求页容量，不保证每页实际返回相同条数 |
| total_records | 已成功写入记录总数 |
| files | 当前保存完整路径字符串 |
| schema_version | 固定字符串 0.1；不是自动读取领域规则版本 |

成功返回 0；捕获 NotImplementedError 返回 2；捕获列出的 I/O、配置、校验和运行异常返回 1。`raise SystemExit(main())` 将返回值传给操作系统。argparse 在 main 的 try 之外执行，所以 `--help` 或无效 CLI 参数可能直接触发 SystemExit，而不走后面的 except。

若第二页校验失败，第一页可能已存在，而本次不会进入最终 manifest 写入。旧目录中原先已有的 manifest 不会自动消失，写 manifest 本身失败也可能留下部分文件。因此“失败不生成成功摘要”仅描述正常控制流，不能推导出目录一定干净。当前建议每次采集选择新目录。

## 8. Semantic specification 数据模型

源码：[preference_query.py](D:/Study/anime-preference-sft/src/anime_pref/schemas/preference_query.py)

### 8.1 SetConstraintSpec

三个 tuple：all_of、any_of、none_of，默认都为空。

- all_of：同时要求列表内所有标签。
- any_of：要求列表内至少一个标签。
- none_of：排除列表内所有标签。

这是查询字段设计的解释；实际检索执行器尚未实现。不要用当前 builder 成功返回来证明数据库中一定存在满足查询的作品。

### 8.2 RangeConstraintSpec

min/max 为 `int | None`，文档约定包含边界。例如 min=12、max=24 表达闭区间。模型本身不校验，检查发生在 builder。

### 8.3 SemanticSpec

| 字段 | 输入类型 | 结果去向 |
|---|---|---|
| genres、tags | SetConstraintSpec | hard_constraints 同名字段 |
| tag_groups | SetConstraintSpec | 展开到 tags；自身不输出 |
| year、episodes | RangeConstraintSpec | hard_constraints 的范围对象 |
| formats、status | tuple[str, ...] | 排序后的 JSON 数组 |
| reference_titles | tuple[str, ...] | 顶层数组；保持字符串和顺序 |
| soft_preferences | tuple[str, ...] | 同上 |
| unresolved_preferences | tuple[str, ...] | 同上 |

`field(default_factory=SetConstraintSpec)` 为每个新 spec 创建自己的默认对象。没有输入的偏好保持空值，不会从参考作品补充 genre/tag。soft 与 unresolved 的归类已在输入里决定，builder 不负责判断一句话应该放在哪一个字段。

## 9. 领域规则加载

源码：[query_builder.py](D:/Study/anime-preference-sft/src/anime_pref/data/query_builder.py)，配置：[domain_rules.v0.1.json](D:/Study/anime-preference-sft/configs/domain_rules.v0.1.json)

### 9.1 DomainRules

包含 schema_version、genres/tags/formats/statuses 四个 frozenset，以及 `tag_groups: Mapping[str, tuple[str, ...]]`。注意规则字段叫 statuses，spec 和 Gold 字段叫 status。

当前配置包含 19 个 genre、3 个 Harem tag、7 个 format、5 个 status。它是受限的本地允许列表，不是由本次采集自动生成的完整 taxonomy。单页作品校验允许 `Time Loop`，而当前 Gold builder 会因 tags allowlist 未包含它而拒绝；两层职责不同。

### 9.2 load_domain_rules(path) → DomainRules

1. UTF-8 读取、json.loads 解析；I/O、Unicode、JSON 解码错误包装为 ValueError 并保留原因。
2. 检查顶层为 dict，键集合精确为 schema_version/taxonomy/tag_groups。
3. schema_version 必须为非空字符串。当前并未限制只能等于 0.1。
4. taxonomy 必须为 dict，精确包含四种 allowlist。
5. 调用 validate_allowlist 清理和验证每个列表。
6. 逐组验证 tag_groups，清理组名、拒绝清理后重复组名，验证组成员。
7. 组内所有 tag 必须已在 tags allowlist 中。
8. 将 allowlist 转 frozenset、组成员转 tuple、组映射包装 MappingProxyType。

### 9.3 内部函数 validate_allowlist(name, value)

name 只用于错误定位，例如 `taxonomy.formats`。value 必须为非空 list。每项必须是非空白字符串，然后 strip，最后才检查重复。否则 `"TV"` 和 `" TV "` 会在 frozenset 中静默合并。

返回值是清理后的 list，外层再转 frozenset。规则文件允许这种空格清理；semantic spec 的 taxonomy 值采用精确匹配，不使用同一清理策略。

边界：JSON 重复对象键会被默认 json.loads 先覆盖，加载器没有 duplicate-key hook。直接手工构造 DomainRules 可以绕过加载器验证；frozen 和类型标注无法代替运行时规则检查。

## 10. 确定性 Gold JSON 构建

### 10.1 build_query(spec, rules) → dict

此函数是纯内存转换，不联网、不读写文件。先要求 spec 为 SemanticSpec、rules 为 DomainRules，然后校验各字段、展开批准规则、组装完整对象。

“确定性”意味着相同的有效输入和相同规则产生相同结果；hard 枚举列表即使输入顺序不同，也会排序成相同输出。文本顺序不会被归一化，因此不同文本顺序仍会产生不同输出。

### 10.2 validate_string_tuple()

参数为 value、name、可选 allowlist 和必需的 sort_output。

执行顺序：tuple 类型检查 → 元素字符串/非空白检查 → 精确重复检查 → allowlist 成员检查 → 复制为 list → 按选项排序。

`strip()` 只用于判断字符串是否为空，不把 strip 结果写回。`" Mystery "` 因无法精确命中 allowlist 而被拒绝；文本字段中的 `"  节奏紧凑  "` 则会保留原始空格。

先检查元素类型再使用 set，避免某个元素为 list 时在 set 构造处抛出无关 TypeError。当前不允许用 list 替代输入 tuple，即便两者包含相同字符串。

### 10.3 validate_set_constraint()

要求输入为 SetConstraintSpec，对每个 operator 调用上述辅助函数并排序。随后构造三组 set，通过交集检测 all/any、all/none、any/none 的重叠。

集合交集表达“这两组是否有同一个值”，与直接比较列表顺序无关。任何重叠都被当前代码拒绝。

这里的拒绝规则比逻辑矛盾检测更严格：同一个标签同时出现在 all_of 和 any_of 可能只是冗余，并不必然矛盾；any_of 中一部分同时被 none_of 排除，也可能仍有其他可满足选项。当前规范主动排除这类输入，以简化 canonical spec。这是数据设计政策，不是通用布尔逻辑定律。

### 10.4 Harem 组展开

先验证直接 tags，再验证 tag_groups 中的组名属于 rules.tag_groups。每个 operator 按如下步骤处理：

```python
combined = list(tags[operator])
for group_name in tag_groups[operator]:
    combined.extend(rules.tag_groups[group_name])
# 检查重复，然后排序
```

采用 list 拼接保留重复，目的是先发现问题，而不是用 set.update 静默去重。若显式排除 Female Harem，又排除 HAREM，展开后相同 tag 出现两次，函数拒绝。展开完成后再次检查跨 operator 交集。

当前 HAREM 的三个成员是 Female Harem、Male Harem、Mixed Gender Harem。none_of 展开为三个排除项，any_of 展开为三个候选项。all_of 也会展开为三个同时要求项；这不应直接解释成普通自然语言“想看后宫”，其语义需要理论分支确认。

### 10.5 validate_range()

要求 RangeConstraintSpec；逐个检查 min/max 是 int 或 None，排除 bool；两者都有值时检查 min 不大于 max；返回新的 `{"min": ..., "max": ...}`。

这里没有设合法年份上下界或非负集数政策。因此负值可通过。它也不根据“一季”“短篇”等词计算集数，输入必须已经给出 canonical 数值。

### 10.6 返回值和副作用

genres/tags、范围、formats/status 放进 hard_constraints；三个文本字段放顶层。未表达项全部保留为空数组或 null，tag_groups 不进入结果。

输出 list/dict 是可变对象，但函数没有就地修改 spec。若调用者随后破坏 query，dumps_query 会重新检查结构。规则由正常加载器得到时，组内 tag 已校验；build_query 不会完整重验人工构造的 DomainRules。

## 11. Canonical JSON 序列化

### 11.1 dumps_query(query) → str

它接受已经构造的 query，返回 JSON 文本，不负责落盘。它也不接收 DomainRules，所以不能独立证明某个标签属于 AniList taxonomy。

执行路径：精确键和顺序检查 → genres/tags 内容检查 → 范围检查 → formats/status 检查 → 三个文本字段检查 → json.dumps。

### 11.2 require_mapping_with_keys()

检查对象为 Mapping，并比较 `tuple(value.keys())` 与固定键序列。一次比较会拒绝缺失键、额外键及相同键但顺序不同的对象。顶层、hard_constraints、operator 对象、range 对象分别使用各自的序列。

JSON 对象在语义上通常不依赖键顺序；这里的顺序是项目当前的 canonical 文本规范，不应把它解释成通用 JSON 语法要求。

### 11.3 require_string_list()

输出侧要求 list，不接受输入侧的 tuple。检查非空白字符串、重复值，hard 字段还要求已经排序。它只验证，不替调用者重新排序。文本字段不要求排序，保持用户明确提供的顺序。

### 11.4 其他检查与 JSON 参数

genres/tags 重复执行跨 operator 重叠检查；year/episodes 再验证整数、非 bool、min<=max。随后使用：

```python
json.dumps(query, ensure_ascii=False, separators=(",", ":"), allow_nan=False)
```

ensure_ascii=False 保留中文；separators 去掉对象/数组语法分隔符后的额外空格；allow_nan=False 禁止非标准 NaN/Infinity。它不会删除字符串内容里的空格、逗号或冒号。编码失败的 TypeError/ValueError 包装成带原因的 ValueError。

边界：Mapping 检查允许 MappingProxyType 等对象通过前面的检查，但标准 json.dumps 不一定支持这些对象，会在末尾报 ValueError。builder 正常返回的是普通 dict，因此正常链路不受影响。结构正确且排序的未知标签仍可能通过本函数；taxonomy 校验责任在 builder。

## 12. 约束结构签名

源码：[constraint_signature.py](D:/Study/anime-preference-sft/src/anime_pref/data/constraint_signature.py)

### 12.1 build_constraint_signature(query) → str

只读取 hard_constraints。先检查顶层有该字段，hard 为 dict 且字段集合完整；genres/tags 各有三种 operator；year/episodes 各有 min/max；formats/status 为字符串 list。

内部 `check_string_list(val, name)` 检查 list 和各元素的 str 类型，但不检查空字符串、重复或排序。

然后生成 token 到布尔值的映射：列表用 `bool(list)` 表示是否非空，边界使用 `is not None`。后者不能换成 `bool(bound)`，否则 0 会被误判成没有边界。

```text
GENRE_ALL → GENRE_ANY → GENRE_NONE
→ TAG_ALL → TAG_ANY → TAG_NONE
→ YEAR_MIN → YEAR_MAX
→ EPISODE_MIN → EPISODE_MAX
→ FORMAT → STATUS
```

按 SIGNATURE_ORDER 挑出启用 token，用 `" + "` 拼接；没有 token 返回 NO_HARD_CONSTRAINT。

例如 Mystery 或 Sci-Fi 加至少 2010 年，产生 `GENRE_ANY + YEAR_MIN`。换成其他 genre 或其他年份仍可能得到同一签名。它是结构统计标签，不是样本 ID、内容哈希或防泄漏保证。

本函数校验比 dumps_query 弱：不检查 min<=max、空文本、重复、冲突、排序或 taxonomy。它也不读取 reference/soft/unresolved。推荐理解为“对已验证 query 提取统计特征”，不能替代完整验证器。

## 13. 贯穿示例：从规格到输出

以下为可在项目根目录启动 Python 后运行的示例；不访问 API，不写数据文件：

```python
import sys
from pathlib import Path
sys.path.insert(0, str(Path.cwd() / "src"))

from anime_pref.schemas.preference_query import (
    SemanticSpec, SetConstraintSpec, RangeConstraintSpec,
)
from anime_pref.data.query_builder import (
    load_domain_rules, build_query, dumps_query,
)
from anime_pref.data.constraint_signature import build_constraint_signature

rules = load_domain_rules(Path("configs/domain_rules.v0.1.json"))
spec = SemanticSpec(
    genres=SetConstraintSpec(any_of=("Sci-Fi", "Mystery")),
    tag_groups=SetConstraintSpec(none_of=("HAREM",)),
    year=RangeConstraintSpec(min=2010),
    episodes=RangeConstraintSpec(max=24),
    formats=("TV",),
    status=("FINISHED",),
    reference_titles=("Steins;Gate",),
    soft_preferences=("节奏紧凑",),
    unresolved_preferences=("想看有宿命感的作品",),
)
query = build_query(spec, rules)
serialized = dumps_query(query)
signature = build_constraint_signature(query)
print(serialized)
print(signature)
```

三个关键变化：输入 tuple 变成 JSON 数组；Sci-Fi/Mystery 排序；HAREM 变成三个实际 tag。year.max 和 episodes.min 保持 null，没有凭空补齐范围。

便于阅读的预期 query：

```json
{
  "hard_constraints": {
    "genres": {"all_of": [], "any_of": ["Mystery", "Sci-Fi"], "none_of": []},
    "tags": {"all_of": [], "any_of": [], "none_of": ["Female Harem", "Male Harem", "Mixed Gender Harem"]},
    "year": {"min": 2010, "max": null},
    "episodes": {"min": null, "max": 24},
    "formats": ["TV"],
    "status": ["FINISHED"]
  },
  "reference_titles": ["Steins;Gate"],
  "soft_preferences": ["节奏紧凑"],
  "unresolved_preferences": ["想看有宿命感的作品"]
}
```

预期 signature：

```text
GENRE_ANY + TAG_NONE + YEAR_MIN + EPISODE_MAX + FORMAT + STATUS
```

JSON 上面的缩进仅用于阅读；dumps_query 实际输出紧凑单行。此示例手工明确指定语义，不能作为已经完成自然语言生成或抽取能力的证据。

## 14. 逐项阅读测试

源码：[test_scaffold.py](D:/Study/anime-preference-sft/tests/test_scaffold.py) 和 [test_query_builder.py](D:/Study/anime-preference-sft/tests/test_query_builder.py)。测试共有 15 个方法；方法内部的多个断言或 subTest 不会分别增加这个方法计数。

### 14.1 unittest 的读法

`TestCase` 中以 test_ 开头的方法由 discover 收集。assertEqual 验证值相同；assertIsNone 检查 None；assertRaises 验证代码块抛异常；assertRaisesRegex 还检查异常消息。

`copy.deepcopy()` 为异常案例复制嵌套 query，避免修改基准输入后影响其他案例。subTest 给一组边界案例加名称，失败时容易定位。TemporaryDirectory 在测试结束后清理其临时目录；测试不把模拟数据写入项目 raw 目录。

### 14.2 test_scaffold.py：5 个方法

**test_invalid_page_rejected_before_network**：调用 fetch_anime_page(0,10)，期待 ValueError。证明 page=0 提前失败，不覆盖全部 page/per_page 边界。

**test_graphql_errors_with_http_200**：MagicMock 模拟 response，status=200，read 返回含 errors 的 JSON 字节。`response.__enter__.return_value=response` 使 `with urlopen(...) as response` 拿到这个对象。patch 替换客户端使用的 urlopen，函数必须抛含 GraphQL errors 的 RuntimeError。这是 HTTP 成功但应用查询失败的回归案例。

**test_nullable_metadata_and_invalid_tag_rank**：有效 Media 验证 None 保留、tag 内容正确；rank=101 失败；顶层 list 失败。它没有遍历所有 Media 字段的全部错误组合。

**test_jsonl_round_trip_and_no_overwrite**：用 iter(records) 检查写入不依赖 len；读取每行 JSON 与原始记录比较；确认中文、count=2；重复写入报 FileExistsError；空输入返回0；manifest 读回一致、中文与缩进存在、返回 None、重复写失败。没有模拟磁盘写满或序列化中断。

**test_fetch_script_end_to_end_without_network**：通过 importlib.util 从脚本路径创建模块 spec，再 exec_module 导入。模块名不是 `__main__`，所以不会自动触发脚本入口。调用 main 前 patch sys.argv、fetch_anime_page、sleep 和 print。

内部 **fake_fetch(page, per_page, *, endpoint, timeout_seconds)** 记录调用参数，根据页码返回预设响应；第1页有后续，第2页无后续。传入 --pages 5，但只应请求1、2页。断言一次 sleep、返回0、原始 JSONL 完全一致、manifest 数量/版本/UTC 后缀。它替换了 fetch_anime_page，因此覆盖脚本到 schema/I/O，不覆盖真实 HTTP 栈。endpoint/timeout 被记录但没有逐值断言，print 只检查调用一次，没有检查完整提示文案。

### 14.3 test_query_builder.py：10 个方法

**test_semantic_spec_defaults_are_explicitly_empty**：检查 genres/year/reference 的默认值；不是逐字段穷尽检查。

**test_load_domain_rules_validates_approved_taxonomy**：读取真实本地规则，确认版本、Mystery、HAREM tuple 和成员归属，尝试写入新 group 必须 TypeError。

**test_load_domain_rules_rejects_non_object_and_normalized_duplicates**：临时生成四个无效配置：顶层 list、清理后重复 format、清理后重复 group、组成员不在 tags allowlist。每个必须 ValueError。

**test_build_query_expands_harem_and_preserves_only_expressed_semantics**：手写 SemanticSpec 和完整 expected 字典，对比 Harem 排除、排序、年份、参考标题、soft 字段和全部空字段。字典相等本身不比较键顺序；序列化器另行约束顺序。`assertNotIn('tag_groups', query)` 只检查顶层，但完整 expected 对比还限制了嵌套输出结构。

**test_builder_rejects_unknown_taxonomy_and_conflicting_constraints**：分别验证未知 genre、all/none 冲突、反向年份区间、bool 集数以及普通 dict spec 被拒绝。

**test_builder_preserves_text_and_rejects_redundant_group_expansion**：验证 format 排序、status 数组、带首尾空格的 soft 文本原样保留，以及直接 tag 加同 operator 的 HAREM 导致展开重复时失败。

**test_dumps_query_uses_canonical_unicode_json**：解析序列化结果后等于 query，中文仍在，特定样本里不出现 `": "` 或 `", "`。此断言对这个样本成立；若业务文本本身含这些字符组合，不能直接用同样的全字符串断言判断紧凑格式是否合格。

**test_dumps_query_rejects_invalid_structure_order_and_bounds**：额外顶层键、顶层错误顺序、operator 值为 tuple、bool bound、NaN bound，均应 ValueError。尚未穷尽 Infinity、每一层键缺失、每种冲突。

**test_constraint_signature_uses_fixed_structural_order**：验证三个 token 组合 `GENRE_ANY + TAG_ALL + EPISODE_MAX`。它不单独覆盖所有12个 token。

**test_constraint_signature_handles_empty_and_rejects_bad_structure**：空 query 和仅 soft 的 query 得到 NO_HARD_CONSTRAINT；缺 status 或 formats 为字符串失败。

### 14.4 如何理解“全部通过”

它证明已写断言对应的实现行为没有观察到回归。它不能证明自然语言语义正确、taxonomy 全量完整、Harem 任意组合可满足、训练数据无泄漏或真实网络永远可用。当前测试没有测量覆盖率，不能声称100%覆盖。

## 15. 运行与排查

### 15.1 只读配置检查与测试

```powershell
cd D:\Study\anime-preference-sft
python -B scripts/fetch_anilist.py --dry-run
python -B -m unittest discover -s tests -v
```

`-B` 避免导入时生成新的 bytecode 缓存；测试里的临时 I/O 仍会创建和清理临时文件。只运行结构化目标模块测试：

```powershell
python -B -m unittest discover -s tests -p "test_query_builder.py" -v
```

本次文档核验的测试结果见文末“核验记录”。

### 15.2 可选真实采集

以下命令会访问网络并创建文件；阅读文档本身不会运行它。output 应选择尚未使用的目录，示例名称若已经存在应更换。

```powershell
python -B scripts/fetch_anilist.py --pages 1 --per-page 2 --output data/raw/learning-run-001
```

### 15.3 常见错误定位

| 现象 | 先检查 | 不应采取的处理 |
|---|---|---|
| FileExistsError | 是否复用已有页文件/manifest | 改成 w 模式覆盖缓存来绕过 |
| GraphQL errors | 查询字段、变量和服务返回消息 | 将错误当成空 media |
| spec must be a SemanticSpec | 是否传了普通 dict | 增加模糊兼容格式掩盖契约 |
| genres must be a SetConstraintSpec | 嵌套模型是否正确 | 只转换最外层字典 |
| must be a tuple | 是否漏写单元素逗号或用了list | 修改规则以接受任意容器 |
| outside its allowlist | 精确字符串及本地配置范围 | 根据近似词自动猜标签 |
| duplicates after group expansion | 直接tag和组展开是否冗余 | 静默set去重而不审查输入 |
| keys ... in that order | query构造顺序是否被改变 | 认为所有JSON对象都需要这种顺序 |
| 中文终端乱码 | Python stdout和终端解码配置 | 未检查就重写原始数据 |

终端乱码不能单凭视觉断言文件未损坏；应按 UTF-8 读取文件、比较字符串或检查码点。测试对特定中文样本的包含断言只能证明该案例，不替代对每个输出的检查。

## 16. 实现限制与理论审查问题

下面是阅读时必须保留的边界，文档没有替这些问题作新决定。

1. **Harem operator 语义。** 同 operator 展开是当前行为；HAREM 放在 all_of 会要求三种 tag 同时存在。多组 OR 概念若分别都必须满足，简单扁平 any_of 也未必能表示，需要理论上明确逻辑结构。
2. **交叉重复政策。** 当前统一拒绝交叉重叠和组展开冗余；这并不等于所有这些输入都逻辑矛盾。
3. **业务数值范围。** year/episodes 只有类型和顺序验证，没有业务上下界；检索时缺失年份/集数如何处理尚未落地。
4. **Tag rank。** 采集保留 rank，但偏好 Query 没有阈值，也没有实现 rank 匹配策略。不能把 label 存在等同于推荐时的强匹配。
5. **taxonomy来源。** 本地配置是当前有限白名单，未保存自动获取的完整 taxonomy snapshot、来源时间或版本哈希；不能宣称完整审计。
6. **formats/status。** 当前为数组，候选值之间的执行语义还需随推荐工具确认；当前没有执行器。
7. **显式空字段和键序。** 属于当前工程规范，是否用于最终模型训练目标仍应遵循理论审查结果。
8. **文本字段政策。** 原样保存、禁止完全重复、不自动移动 soft/unresolved；reference 不解析成 AniList ID，不做同名消歧。
9. **版本约束。** loader 接受任何非空版本字符串，builder 不强制0.1，Gold本身没有版本字段。未来应在样本元数据层明确版本归属。
10. **签名不防泄漏。** 值、数量、模板家族和规则来源不同的样本可能共享signature；它不能替代generation family切分。
11. **验证边界不一致。** loader、builder、serializer、signature 各检查不同范围，后两者不能替代 taxonomy 验证。
12. **缓存非事务性。** 中断可能保留部分分页文件或不完整manifest，没有自动恢复或回滚。
13. **测试通过的边界。** 尚未覆盖所有网络/磁盘失败、输入组合、版本兼容性，未验证训练或推荐效果。

之前“工作块完成”的准确解释是：当前规格下函数已实现、指定测试已通过；不是理论语义已经获得最终批准。





本次仅整理学习文档，没有修改实现代码或测试代码，没有发起真实 AniList 请求。测试耗时是本次观测值，后续运行可能不同。后续函数行为变化时，应同步更新对应章节、示例和这份核验记录。
